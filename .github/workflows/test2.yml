name: run .py script please

on:
  schedule:
    - cron:  "* * * * *"
  workflow_dispatch:
  

jobs:
  update-wiseoldman:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - run: pwsh script.py
